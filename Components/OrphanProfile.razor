<div class="md:flex ml-8 mr-8 mt-4 mb-8 bg-blue-100 p-4 items-center justify-between">
    <div class="">
        <h3 class="text-blue-600 text-2xl" font-bold>@Orphan.FirstName's Profile</h3>
    </div>
    <div class="">
        <button @onclick="ToggleEditMode" class="bg-transparent hover:bg-blue-500 text-blue-700 font-semibold hover:text-white py-2 px-4 border border-blue-500 hover:border-transparent rounded">
            <div class="flex items-center">
                <i class="@editViewIcon mr-2"></i> @editViewText
            </div>
        </button>
    </div>
</div>


@if (editMode)
{
    <div class="mb-20">
        <OrphanProfileEdit Orphan="@Orphan" HandleOrphanEdited="@HandleOrphanEditedByChild"/>
    </div>
}
else
{
    <div class="mb-20">
        <OrphanProfileView Orphan="@Orphan"/>
    </div>
}